# Generated by gardengenie -- this file will be overwritten
# This file describes the network interfaces available on your system
# and how to activate them. For more information, see interfaces(5).

# The loopback network interface
auto lo
iface lo inet loopback


# The primary network interface
auto eth0
iface eth0 inet dhcp

{% if not no_wifi %}
# WiFi
auto {{ wifi_interface }}
{% if not disable_wifi %}iface {{ wifi_interface }} inet dhcp  {# assumes dhcp #}
    {% if 'wpa' == wifi_encryption %}wpa-ssid "{{ wifi_ssid }}"
    wpa-psk "{{ wifi_password }}"
    {% elif 'wep' == wifi_encryption %}wireless-essid {{ wifi_ssid }}
    wireless-key {{ wifi_password }}
    {% else %}wireless-essid {{ wifi_ssid }} {# default to no encryption #}
    wireless-mode managed
{% endif %}{% endif %}
{% endif %}

auto wap0 {# virtual access point #}
iface wap0 inet static
    hostapd /etc/hostapd/hostapd.conf
    address 192.168.8.1
    netmask 255.255.255.0
